
version: 2

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          # this tests if the column in the table doesn't 
          # contain any duplicates and are strictly unique values
          - unique
          # this tests if the the column in table doesn't contain any nulls 
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null

    # we could basically rewrite the tests into this
    # format also
    # tests:
    #   - unique:
    #       columns: [id]
    #   - not_null:
    #       columns: [id]

    
